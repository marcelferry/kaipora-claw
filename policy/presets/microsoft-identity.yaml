version: 1
network_policies:
  microsoft_identity:
    name: microsoft-identity
    endpoints:
      - host: login.microsoftonline.com
        port: 443
      - host: login.live.com
        port: 443
    binaries:
      - path: /usr/bin/curl
      - path: /usr/bin/node
      - path: /bin/bash
